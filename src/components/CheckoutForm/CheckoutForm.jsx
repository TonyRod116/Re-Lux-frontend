// import { purchaseIntent } from './services/stripe';

// async function initPayment() {
//   const clientSecret = await purchaseIntent(5000); // amount in cents
//   // use clientSecret with Stripe's confirmPayment API
// }
